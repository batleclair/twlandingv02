<div class="flex-center">
  <% if !user_signed_in? %>
    <%= link_to "Candidater",  new_user_registration_path, class: "button full-width btn-main-color no-wrap" %>
  <% elsif current_user.applied?(offer) %>
    <button class="button full-width btn-main-color no-wrap" data-action="click->candidacy#popup2" data-id=<%=offer.id%>>Candidater</button>
  <% else %>
    <p class="text-main sm-text center-text">Merci pour votre candidature 🙌</p>
  <% end %>
</div>
