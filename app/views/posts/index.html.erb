<% content_for :title, "Le blog de Demain" %>
<% content_for :description, "Retrouvez tous nos articles. On y parle sans langue de bois du turfu du monde du travail, de gentilles entreprises et de l'urgence de de bosser pour l'intérêt général avant que ce soit trop tard !" %>

<div class="white-wrapper-narrow-content">
  <h1 class="d-none">Demain Works : un blog sur le mécénat de compétences, les associations et le "Future of Work"</h1>
  <h2 class="center-text primary-text-color mb-5 mt-5">Le blog de <span class="stress">Demain</span> 🙌</h2>

  <h5 class="center-text mb-3">Retrouvez toutes nos publications</h5>
  <h6 class="center-text">On y parle du futur du travail, de gentilles entreprises et de l'urgence de de bosser pour l'intérêt général avant qu'il ne soit trop tard 🤯</h6>
  <div class="row">

    <% @posts.each do |post| %>
      <div class="col-xxl-4 col-lg-6 col-md-6 col-sm-12 ps-4 pe-4 mt-5">
        <% bg_img = post.photo.attached? ? cl_image_path(post.photo.key) : image_path('bannerbg.jpg') %>
        <%= link_to(article_path(slug: post.slug)) do %>
          <div class="post-card">
            <div class="upper-post-card" style="background-image: linear-gradient(45deg, rgba(0, 0, 0, 0.4), rgba(0, 0, 0, 0.6)), url('<%= bg_img %>')" >
              <% unless post.category_list.blank? %>
                <h6><%= post.category_list[0] %></h6>
              <% end %>
              <span><%= post.published_ago %></span>
            </div>
            <div class="lower-post-card">
              <h5 class="primary-text-color mb-2"><%= post.title %></h5>
              <p><%= post.content.to_plain_text.truncate(200, omission: '') %></p>
              <div class="d-flex justify-content-end align-items-center">
                <i class="fa-solid fa-arrow-right-long me-2"></i><strong>lire l'article</strong>
              </div>
            </div>
          </div>
        <% end %>


      </div>
    <% end %>
  </div>

  <div class="flex-resp justify-content-center align-items-center mt-5" data-controller="contact">
    <h6 class="primary-text-color m-2 center-text">Pour rester informé·e de nos actus : </h6>
    <a class="button btn-flag-color" href="" data-action="click->contact#popup">Inscription</a>
    <%= render partial: "pages/contact" %>
  </div>
</div>
