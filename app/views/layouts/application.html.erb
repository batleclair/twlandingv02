<!DOCTYPE html>
<html>
  <head>
    <title><%= yield(:title).present? ? yield(:title) : meta_title %></title>
    <meta name="description" content="<%= yield(:description).present? ? yield(:description) : meta_description %>">
    <%= favicon_link_tag %>

    <!-- Facebook Open Graph data -->
    <meta property="og:title" content="<%= yield(:title).present? ? yield(:title) : meta_title %>" />
    <meta property="og:type" content="website" />
    <meta property="og:url" content="<%= request.original_url %>" />
    <meta property="og:image" content="<%= meta_image %>" />
    <meta property="og:description" content="<%= yield(:description).present? ? yield(:description) : meta_description %>" />
    <meta property="og:site_name" content="<%= meta_title %>" />

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>

    <!-- Google tag (gtag.js) -->
    <script async src="https://www.googletagmanager.com/gtag/js?id=G-Z6KJ7NN8FL"></script>
    <script>
      window.dataLayer = window.dataLayer || [];
      function gtag(){dataLayer.push(arguments);}
      gtag('js', new Date());

      gtag('config', 'G-Z6KJ7NN8FL');
    </script>

  </head>

  <body style="background-image: url('<%= image_path "bg2.png" %>')" data-controller="modal" data-action="keyup->modal#close">
    <%= render partial: "shared/menu" %>
    <%= yield %>

    <% if flash.any? %>
      <% flash.each do |notice, msg| %>
      <div>
      <p class="notice"><%= msg %></p>
      </div>
      <% end %>
    <% end %>

    <%= render partial: "shared/footer" %>

  </body>
</html>
