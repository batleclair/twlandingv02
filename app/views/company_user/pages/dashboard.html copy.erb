<div data-controller="popup">
  <h5 class="mb-30 fw-7 text-main">👋 Bonjour <%= current_user.first_name %></h5>

  <div class="flex-between max-w-750 w-100">
    <div class="max-w-400">
      <h6 class="mb-10 fw-6">Mes demandes</h5>
      <div class="flex-center-left mb-10">

        <%= link_to user_employee_application_path(current_user.last_employee_application), data: {turbo_frame: "modal"}, class:"text-main min-w-30" do %>
          <i class="fa-solid fa-circle-question text-main fs-20"></i>
        <% end %>
        <% if current_user.eligibility == "pending" %>
          <p>Votre demande est en attente de validation par <%= current_user.company.name %>, vous ne pouvez pas encore candidater à des missions</p>
        <% else %>
          <p>Vous êtes actuellement autorisé par <%= current_user.company.name %> à vous engager sur une mission</p>
        <% end %>
      </div>
      <% if current_user.candidate.call_status_booked? %>
        <div class="ml-30 mt-20">
          <h6>Un call est programmé avec Demain</h6>
          <p>L'objectif : définir ensemble le type de mission et d'association qui vous correspond !</p>
        </div>
      <% end %>
    </div>

    <div class="max-w-200 w-100 ml-40">
      <h6 class="mb-10 fw-6">Consulter les offres</h5>
      <%= link_to "Favoris", user_bookmarks_path, class: "round d-block bg-shade-to-dark text-main w-100 text-center pt-05 pb-05 hover-2" %>
      <div class="mb-10"></div>
      <% if current_user.eligibility_on_going? %>
        <%= link_to "Recommandations", user_selections_path, class: "round d-block bg-shade-to-dark text-main w-100 text-center pt-05 pb-05 hover-2" %>
        <div class="mb-10"></div>
      <% end %>
      <%= link_to "Toutes les offres", user_offers_path, class: "round d-block bg-shade-to-dark text-main w-100 text-center pt-05 pb-05 hover-2" %>
    </div>
  </div>


  <div class="mt-30">
    <% if @active_engagement %>
      <div>
        <h6 class="mb-10 fw-6"><%= @active_engagement.class == Candidacy ? "Candidature" : "Mission" %> principale</h5>
        <%= render partial: "company_user/shared/engagement_card" %>
      </div>
    <% else %>
      <h6 class="mb-10 fw-6 mt-30">En attente</h5>
      <div class="dashboard-grid max-w-750">
        <%= link_to user_selections_path, class:"round-20 first-main-border bg-white-to-shade hover-2" do %>
          <div class="w-100 p-20 d-flex pe-none h-100">
            <h5 class="text-main fw-7 fs-30 min-w-30"><%= 5 %></h5>
            <p>recommendations attendent votre réponse</p>
          </div>
        <% end %>

        <%= link_to user_candidacies_path, class:"round-20 first-main-border bg-white-to-shade hover-2" do %>
          <div class="w-100 p-20 d-flex pe-none h-100">
            <h5 class="text-main fw-7 fs-30 min-w-30"><%= 1 %></h5>
            <p> candidatures en cours auprès d'associations</p>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>


  <%= turbo_frame_tag "modal" %>
</div>
