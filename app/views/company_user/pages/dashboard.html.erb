<div data-controller="popup">
  <h5 class="mb-40 fw-7 text-main">👋 Bienvenue <%= current_user.first_name %></h5>

  <div class="flex-stretch-left mb-40">
    <div class="w-100 max-w-400 p-20 bg-shade-to-dark hover-2 shadow-xs position-relative">
      <%= link_to "", user_employee_application_path(current_user.last_employee_application), data: {turbo_frame: "modal"}, class: "h-100 w-100 position-absolute" %>
      <h6 class="mb-10">Vous avez candidaté auprès de votre entreprise :</h6>
      <div class="flex-center-between">
        <% if current_user.eligibility == "pending" %>
          <p class="mr-10">⏳</p>
          <p>Votre demande est en attente de validation par <span class="text-main fw-6"><%= current_user.company.name %></span>, vous ne pouvez pas encore candidater à des missions</p>
        <% else %>
          <p class="mr-10">✔️</p>
          <p>Votre demande a été acceptée par <span class="text-main fw-6"><%= current_user.company.name %></span> ! Vous pouvez candidater à des missions</p>
        <% end %>
      </div>
    </div>
  </div>

  <% if current_user.candidate.call_status_booked? %>
    <div class="mb-40">
      <p>Un call est programmé avec votre conseiller pour définir ensemble le type de mission et d'association qui peut vous correspondre</p>
    </div>
  <% end %>

  <% if @active_engagement %>
    <h5 class="mb-10">Votre engagement en cours :</h5>
    <%= render partial: "company_user/shared/engagement_card" %>
  <% else %>
    <% if current_user.eligibility == "on-going" %>
      <h5 class="mb-10">Vous pouvez candidater aux <%= link_to "Missions", user_offers_path, class:"text-main fw-7" %> dès à présent</h5>
    <% else %>
      <h5 class="mb-10">Vous pouvez parcourir les <%= link_to "Missions", user_offers_path, class:"text-main fw-7" %> dès à présent</h5>
    <% end %>
  <% end %>

  <%= turbo_frame_tag "modal" %>
</div>
