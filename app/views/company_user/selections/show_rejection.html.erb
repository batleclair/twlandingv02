<h5 class="fw-7 text-main mb-40">Suggestions</h5>

<div class="d-flex">
  <%= render partial: "summary" %>
  <div class="ml-40 w-100 max-w-400">
    <div class="w-100">
      <div class="mb-20">
        <h5 class="text-main">Refus</h5>
        <p>Vous n'avez pas souhaité candidater</p>
        <% if @selection.motivation_msg.present? %>
          <h6 class="mt-10 fw-6">Votre commentaire:</h6>
          <table class="max-w-400 mb-20">
            <tbody class="va-top">
              <tr>
                <th class="pr-10">💬</th>
                <th class="pr-10 no-wrap">Votre messsage :</th>
                <td><i>"<%= @selection.motivation_msg %></i> "</td>
              </tr>
            </tbody>
          </table>
        <% end %>
      </div>
      <div class="w-100">
        <%= simple_form_for(@selection, url: user_selection_path(@selection), data: {turbo: false}) do |f| %>
          <%= f.input :active, as: :hidden, input_html: {value: true} %>
          <button type="submit" class="w-100 button round bg-white-to-shade first-main-border text-main bn-none">
            <i class="fa-solid fa-fire mr-10 text-flag-1"></i>
            Vous avez changé d'avis ?
          </button>
        <% end %>
      </div>
    </div>
  </div>
</div>
