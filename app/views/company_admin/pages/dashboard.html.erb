<h5 class="mb-40 fw-7 text-main">👋 Bienvenue <%= current_user.first_name %></h5>

<div class="dashboard-grid ml-20 mr-20 max-w-750">

  <%= link_to company_admin_employee_applications_path(status: :pending), class:"round-20 first-main-border bg-white-to-shade hover-2" do %>
    <div class="w-100 p-20 d-flex pe-none h-100">
      <h5 class="text-main fw-7 fs-30 min-w-30"><%= @pending_applications.count %></h5>
      <p>Demandes d'éligibilité en attente d'approbation</p>
    </div>
  <% end %>

  <%= link_to company_admin_candidacies_path(status: :pending), class:"round-20 first-main-border bg-white-to-shade hover-2" do %>
    <div class="w-100 p-20 d-flex pe-none h-100">
      <h5 class="text-main fw-7 fs-30 min-w-30"><%= @pending_candidacies.count %></h5>
      <p>Candidatures en attente d'approbation</p>
    </div>
  <% end %>

  <%= link_to company_admin_missions_path(view: :in_progress), class:"round-20 first-main-border bg-white-to-shade hover-2" do %>
    <div class="w-100 p-20 d-flex pe-none h-100">
      <h5 class="text-main fw-7 fs-30 min-w-30"><%= @pending_missions.count %></h5>
      <p>Missions en cours de contractualisation</p>
    </div>
  <% end %>
</div>
